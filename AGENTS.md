# 面料微信小程序项目协作说明

本文件用于给后续 Codex 对话提供必要的项目边界。它不会限制与素材仓库导入无关的正常开发；处理普通小程序、后台、客户需求或视觉改版时，仍以用户当前要求和现有代码为准。

## 每次修改前

1. 执行 `git status --short`，保留所有已有修改，不覆盖或丢弃用户工作。
2. 先检查当前代码、数据结构和部署配置，再判断旧文档是否仍适用。
3. 涉及素材仓库同步或后台“新增面料”时，完整阅读 `docs/media-warehouse-import.md`。
4. 不主动提交、推送、部署、迁移或删除生产数据；仅在用户明确授权后执行。

## 素材仓库导入边界

- 素材仓库导入是辅助填写“新增面料”的独立功能，不得改变小程序现有商品编辑、保存、上架和展示流程。
- 图片用途由用户在素材仓库手动选择。小程序后台不得根据文件名、素材分类或图片内容自动分类；同一张图片可以进入轮播图、详情图和电子色卡中的多个分组。
- 导入必须打开空白“新增面料”编辑器并预填资料，不能按货号自动打开旧商品，不能提前写入产品表，不能覆盖同货号商品，也不能自动保存或上架。
- 导入可预填货号、面料名称、简介、成分、克重、门幅及三组图片。分类、色数、标签、推荐用途和最终状态继续由后台用户确认。
- 素材仓库传入的成分使用小程序展示格式：`60%Cotton·23%Rayon·17%Linen`。格式转换在素材仓库发送端完成，小程序后台按普通可编辑文本接收，不要反向改写素材仓库原始数据。
- 一次性导入凭证只允许已登录管理员读取，当前有效期为30分钟且只能消费一次。调整该规则必须得到用户确认。
- 服务端导入接口为 `POST /api/integrations/media-warehouse/imports`；后台消费接口为 `GET /api/product-imports/<token>`。不要把共享密钥或一次性凭证写入前端源码、日志、截图或 Git。
- 共享密钥只从 `HENGYITEX_MEDIA_WAREHOUSE_TOKEN` 读取；公网后台地址从 `HENGYITEX_PUBLIC_ORIGIN` 读取。
- Nginx 的150MB请求上限只能作用于素材仓库导入接口；普通后台上传继续保持16MB限制。

## 修改与验证

- 与本功能无关的需求，不要顺带修改素材仓库导入协议。
- 如确需改变字段映射、图片分组或导入行为，应同时检查素材仓库项目的对应实现，避免两端协议不一致。
- 修改导入后端时至少运行：

```powershell
cd server
python -B -m unittest test_product_imports.py
python -B -m unittest test_image_uploads.py
python -B -m unittest test_homepage.py
node --check ..\admin-web\app.js
```

- 相关部署和环境变量说明见 `docs/media-warehouse-import.md`。
